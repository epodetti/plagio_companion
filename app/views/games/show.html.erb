<h1>Dettagli partita</h1>

<p>
  <strong>Titolo:</strong>
  <%= @game.title %>
  <br>
  <strong>Data:</strong>
  <%= @game.date.strftime("%-d/%-m/%Y, %H:%M") %>
</p>

<%= link_to 'Modifica', edit_game_path(@game) %> |
<%= link_to 'Indietro', games_path %>
