<%= form_with(model: citation, local: true) do |form| %>
  <% if citation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(citation.errors.count, "error") %> prohibited this citation from being saved:</h2>

      <ul>
      <% citation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_title, 'form.round.game.title' %>
    <%= form.number_field :game_title %>
  </div>

  <div class="field">
    <%= form.label :round_number, 'form.round.number' %>
    <%= form.number_field :round_number %>
  </div>

  <div class="field">
    <%= form.label :body, 'Testo' %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
